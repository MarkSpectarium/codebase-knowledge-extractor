---
description: Commit all changes and push to remote repository
model: haiku
allowed-tools: Bash
---

Silently gather git context, analyze changes, stage all files (skip .env/credentials), create commit message with attribution, commit and push. After successful push, output ONLY: "✓ Changes committed and pushed." Nothing else. No explanations, no tool outputs, no commentary.
